title = "Rux Server secret scanning"

[extend]
useDefault = true

[[allowlists]]
description = "Known synthetic documentation and test values"
targetRules = ["generic-api-key"]
stopwords = [
  "replace-with-at-least-24-characters",
  "rux_pat_revoked1",
]
